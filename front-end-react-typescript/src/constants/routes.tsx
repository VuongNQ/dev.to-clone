type Routes =  {
    home: string;
    tags: string;
    about: string;
    latest: string
}

const routes: Routes = {
    home: '/',
    tags: '/tags',
    about: '/about',
    latest: '/latest'
}

export default routes;